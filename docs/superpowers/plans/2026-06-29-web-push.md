# Web Push Уведомления — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить браузерные Push-уведомления для всех ключевых событий платформы.

**Architecture:** VAPID-ключи в .env, Service Worker в `assets/sw.js`, таблица `push_subscriptions` в БД, хелпер `sendPush(userId, ...)` в server.js, UI-кнопка в settings.html.

**Tech Stack:** web-push (npm), Browser Push API, Service Worker API

## Global Constraints

- Рабочая директория: `C:\Users\Админ\source\repos`
- `npm test` должен проходить после каждой задачи
- Push работает только по HTTPS (на localhost с флагом браузера или через продовый домен)
- VAPID ключи генерируются один раз и не меняются (иначе все подписки инвалидируются)
- Ошибки отправки Push — только логировать, не бросать исключение

---

### Task 1: Установить web-push и сгенерировать VAPID ключи

**Files:**
- Modify: `package.json`
- Modify: `env.example`

- [ ] **Step 1: Установить пакет**

  ```bash
  npm install web-push
  ```
  Ожидаемый результат: `web-push` появился в `node_modules` и `package.json`.

- [ ] **Step 2: Сгенерировать VAPID ключи**

  ```bash
  node -e "const wp=require('web-push'); const k=wp.generateVAPIDKeys(); console.log('PUBLIC:', k.publicKey); console.log('PRIVATE:', k.privateKey);"
  ```
  Скопируй оба ключа — вставишь в `.env` на следующем шаге.

- [ ] **Step 3: Добавить ключи в .env**

  Открой `.env` и добавь в конец:
  ```
  # Web Push (VAPID)
  VAPID_PUBLIC_KEY=<вставь публичный ключ>
  VAPID_PRIVATE_KEY=<вставь приватный ключ>
  VAPID_SUBJECT=mailto:noreply@texzakaz.ru
  ```

- [ ] **Step 4: Добавить placeholder в env.example**

  Открой `env.example`, добавь в конец:
  ```
  # Web Push (VAPID) — генерация: node -e "require('web-push').generateVAPIDKeys()" | cat
  VAPID_PUBLIC_KEY=
  VAPID_PRIVATE_KEY=
  VAPID_SUBJECT=mailto:noreply@texzakaz.ru
  ```

- [ ] **Step 5: Commit**

  ```bash
  git add package.json package-lock.json env.example
  git commit -m "feat: добавить web-push, VAPID placeholder в env.example"
  ```

---

### Task 2: Таблица push_subscriptions в БД

**Files:**
- Modify: `db.js` — добавить CREATE TABLE в функцию initDb

**Interfaces:**
- Produces: таблица `push_subscriptions(id, user_id, subscription JSONB, created_at)`

- [ ] **Step 1: Найти место для добавления таблицы в db.js**

  Открой `db.js`, найди блок где создаются другие таблицы (ищи `CREATE TABLE IF NOT EXISTS`). Добавь новую таблицу в тот же блок:

  ```javascript
  await pool.query(`
    CREATE TABLE IF NOT EXISTS push_subscriptions (
      id SERIAL PRIMARY KEY,
      user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
      subscription JSONB NOT NULL,
      created_at TIMESTAMPTZ DEFAULT NOW()
    )
  `);
  ```

- [ ] **Step 2: Проверить**

  ```bash
  npm test
  ```
  Ожидаемый результат: `Static checks passed: ...`

- [ ] **Step 3: Commit**

  ```bash
  git add db.js
  git commit -m "feat: добавить таблицу push_subscriptions для Web Push"
  ```

---

### Task 3: Service Worker

**Files:**
- Create: `assets/sw.js`

**Interfaces:**
- Produces: SW слушает `push` → показывает уведомление; `notificationclick` → открывает URL

- [ ] **Step 1: Создать assets/sw.js**

  ```javascript
  'use strict';

  self.addEventListener('push', (event) => {
      if (!event.data) return;
      let payload;
      try { payload = event.data.json(); }
      catch { payload = { title: 'ТехЗаказ', body: event.data.text(), url: '/' }; }

      event.waitUntil(
          self.registration.showNotification(payload.title || 'ТехЗаказ', {
              body: payload.body || '',
              icon: '/favicon.svg',
              badge: '/favicon.svg',
              data: { url: payload.url || '/' },
              requireInteraction: false,
          })
      );
  });

  self.addEventListener('notificationclick', (event) => {
      event.notification.close();
      const url = event.notification.data?.url || '/';
      event.waitUntil(
          clients.matchAll({ type: 'window', includeUncontrolled: true }).then((list) => {
              for (const client of list) {
                  if (client.url === url && 'focus' in client) return client.focus();
              }
              if (clients.openWindow) return clients.openWindow(url);
          })
      );
  });
  ```

- [ ] **Step 2: Проверить синтаксис**

  ```bash
  node --check assets/sw.js
  ```
  Ожидаемый результат: нет вывода (нет ошибок).

- [ ] **Step 3: Commit**

  ```bash
  git add assets/sw.js
  git commit -m "feat: добавить Service Worker для Web Push уведомлений"
  ```

---

### Task 4: API эндпоинты + хелпер sendPush в server.js

**Files:**
- Modify: `server.js` — добавить require web-push, инициализацию, хелпер, 3 эндпоинта

**Interfaces:**
- Produces:
  - `async function sendPush(userId, title, body, url)` — отправляет push всем подпискам пользователя
  - `GET /api/push/vapid-key` → `{ publicKey: string }`
  - `POST /api/push/subscribe` body: `{ subscription: object }` → `{ ok: true }`
  - `DELETE /api/push/subscribe` → `{ ok: true }`

- [ ] **Step 1: Добавить require web-push в начало server.js**

  Найди блок `require` в начале файла (после других require). Добавь:
  ```javascript
  const webpush = require('web-push');
  if (process.env.VAPID_PUBLIC_KEY && process.env.VAPID_PRIVATE_KEY) {
      webpush.setVapidDetails(
          process.env.VAPID_SUBJECT || 'mailto:noreply@texzakaz.ru',
          process.env.VAPID_PUBLIC_KEY,
          process.env.VAPID_PRIVATE_KEY
      );
  }
  ```

- [ ] **Step 2: Добавить хелпер sendPush**

  Добавь функцию рядом с `sendEmail`:
  ```javascript
  async function sendPush(userId, title, body, url) {
      if (!process.env.VAPID_PUBLIC_KEY) return;
      try {
          const { rows } = await pool.query(
              'SELECT id, subscription FROM push_subscriptions WHERE user_id = $1',
              [userId]
          );
          for (const row of rows) {
              try {
                  await webpush.sendNotification(row.subscription, JSON.stringify({ title, body, url }));
              } catch (e) {
                  if (e.statusCode === 410 || e.statusCode === 404) {
                      await pool.query('DELETE FROM push_subscriptions WHERE id = $1', [row.id]);
                  } else {
                      console.error('[push] send error:', e.message);
                  }
              }
          }
      } catch (e) {
          console.error('[push] sendPush error:', e.message);
      }
  }
  ```

- [ ] **Step 3: Добавить эндпоинты**

  Добавь в server.js (рядом с другими /api/ роутами, например после /api/health):
  ```javascript
  // ===================== WEB PUSH =====================

  app.get('/api/push/vapid-key', (req, res) => {
      if (!process.env.VAPID_PUBLIC_KEY) return res.status(503).json({ error: 'Push не настроен' });
      res.json({ publicKey: process.env.VAPID_PUBLIC_KEY });
  });

  app.post('/api/push/subscribe', requireAuth, async (req, res, next) => {
      try {
          const { subscription } = req.body;
          if (!subscription?.endpoint) return res.status(400).json({ error: 'Неверный subscription объект' });
          await pool.query(
              `INSERT INTO push_subscriptions (user_id, subscription)
               VALUES ($1, $2)
               ON CONFLICT DO NOTHING`,
              [req.user.id, JSON.stringify(subscription)]
          );
          res.json({ ok: true });
      } catch (e) { next(e); }
  });

  app.delete('/api/push/subscribe', requireAuth, async (req, res, next) => {
      try {
          await pool.query('DELETE FROM push_subscriptions WHERE user_id = $1', [req.user.id]);
          res.json({ ok: true });
      } catch (e) { next(e); }
  });
  ```

  > Примечание: `ON CONFLICT DO NOTHING` требует уникального индекса на `(user_id, subscription->>'endpoint')`. Если хочешь дедупликацию — добавь в db.js:
  > ```sql
  > CREATE UNIQUE INDEX IF NOT EXISTS push_subscriptions_user_endpoint
  >   ON push_subscriptions (user_id, (subscription->>'endpoint'));
  > ```
  > Иначе просто убери `ON CONFLICT DO NOTHING` — дубли не страшны, просто пришлёт несколько push.

- [ ] **Step 4: Проверить**

  ```bash
  npm test
  ```
  Ожидаемый результат: `Static checks passed: ...`

- [ ] **Step 5: Commit**

  ```bash
  git add server.js db.js
  git commit -m "feat: Web Push API эндпоинты и хелпер sendPush"
  ```

---

### Task 5: Подключить sendPush к событиям платформы

**Files:**
- Modify: `server.js` — добавить вызовы sendPush в 7 местах

**Interfaces:**
- Consumes: `sendPush(userId, title, body, url)` из Task 4
- Нужно знать `user.id` получателя — в некоторых местах его нужно получать из БД по company

- [ ] **Step 1: Создать хелпер getUserIdByCompany**

  Добавь функцию рядом с другими хелперами:
  ```javascript
  async function getUserIdsByCompany(company) {
      const { rows } = await pool.query(
          'SELECT id FROM users WHERE company = $1',
          [company]
      );
      return rows.map(r => r.id);
  }
  ```

- [ ] **Step 2: POST /api/proposals — уведомить заказчика о новом КП**

  Найди код в `app.post('/api/proposals', ...)` где отправляется email заказчику о новом КП. После строки `await sendEmail(...)` добавь:
  ```javascript
  // Push: уведомить заказчика о новом КП
  getUserIdsByCompany(orderRow.company).then(ids =>
      ids.forEach(id => sendPush(id, 'Новое коммерческое предложение', `«${orderRow.title}» — получен новый отклик`, `${APP_URL}/index`))
  ).catch(() => {});
  ```

- [ ] **Step 3: POST /api/proposals/:id/accept — уведомить поставщика**

  Найди код в `app.post('/api/proposals/:proposalId/accept', ...)`. После отправки email поставщику добавь:
  ```javascript
  // Push: уведомить поставщика о принятом КП
  getUserIdsByCompany(proposalRow.company).then(ids =>
      ids.forEach(id => sendPush(id, 'КП принято!', `Ваше предложение по заявке «${orderRow.title}» принято`, `${APP_URL}/deals`))
  ).catch(() => {});
  ```

- [ ] **Step 4: POST /api/proposals/:id/reject — уведомить поставщика**

  Найди код в `app.post('/api/proposals/:proposalId/reject', ...)`. Добавь:
  ```javascript
  // Push: уведомить поставщика об отклонённом КП
  getUserIdsByCompany(proposalRow.company).then(ids =>
      ids.forEach(id => sendPush(id, 'КП отклонено', `Предложение по заявке «${orderRow.title}» отклонено`, `${APP_URL}/proposals`))
  ).catch(() => {});
  ```

- [ ] **Step 5: POST /api/messages — уведомить получателя**

  Найди async IIFE в `app.post('/api/messages', ...)` где отправляется email. Внутри IIFE после отправки email добавь:
  ```javascript
  // Push: уведомить получателя
  const recipientIds = await getUserIdsByCompany(recipientCompany);
  await Promise.all(recipientIds.map(id =>
      sendPush(id, 'Новое сообщение', `${req.user.company}: ${String(text).slice(0, 80)}`, `${APP_URL}/messages`)
  ));
  ```

- [ ] **Step 6: Cron — дедлайн -3 дня, найди sendDeadlineReminders**

  Найди функцию `sendDeadlineReminders()` в server.js. После `await notifyCompanyEmail(...)` добавь:
  ```javascript
  const reminderIds = await getUserIdsByCompany(order.company);
  await Promise.all(reminderIds.map(id =>
      sendPush(id, 'Дедлайн через 3 дня', `Закупка «${plainTitle(order.title)}» закрывается через 3 дня`, `${APP_URL}/index`)
  ));
  ```

- [ ] **Step 7: POST /api/verification/:id/approve — уведомить компанию**

  Найди роут `app.post('/api/verification/:id/approve', ...)`. После отправки email добавь:
  ```javascript
  // Push: верификация одобрена
  getUserIdsByCompany(verReq.company).then(ids =>
      ids.forEach(id => sendPush(id, 'Верификация одобрена ✓', 'Ваша компания верифицирована платформой ТехЗаказ', `${APP_URL}/settings`))
  ).catch(() => {});
  ```

- [ ] **Step 8: POST /api/auth/register с inviteToken — уведомить команду**

  Найди в `app.post('/api/auth/register', ...)` блок где обрабатывается `inviteToken`. После принятия инвайта добавь:
  ```javascript
  // Push: уведомить команду о новом участнике
  if (invite) {
      getUserIdsByCompany(invite.company).then(ids =>
          ids.forEach(id => sendPush(id, 'Новый участник команды', `${newUser.email} присоединился к вашей компании`, `${APP_URL}/settings`))
      ).catch(() => {});
  }
  ```

- [ ] **Step 9: Проверить**

  ```bash
  npm test
  ```
  Ожидаемый результат: `Static checks passed: ...`

- [ ] **Step 10: Commit**

  ```bash
  git add server.js
  git commit -m "feat: подключить sendPush ко всем событиям платформы (КП, сообщения, дедлайн, верификация, команда)"
  ```

---

### Task 6: Регистрация Service Worker и UI в assets/app.js + settings.html

**Files:**
- Modify: `assets/app.js` — регистрация SW, функции subscribe/unsubscribe
- Modify: `settings.html` — кнопка Push в разделе уведомлений

**Interfaces:**
- Consumes: `GET /api/push/vapid-key`, `POST /api/push/subscribe`, `DELETE /api/push/subscribe`

- [ ] **Step 1: Добавить SW-регистрацию в assets/app.js**

  Найди в `app.js` блок `document.addEventListener('DOMContentLoaded', ...)`. Добавь в конец этого блока:
  ```javascript
  if (hasSession() && 'serviceWorker' in navigator) {
      navigator.serviceWorker.register('/assets/sw.js').catch(e =>
          console.warn('[SW] registration failed:', e.message)
      );
  }
  ```

- [ ] **Step 2: Добавить функции Push-подписки в assets/app.js**

  Добавь в конец `app.js`:
  ```javascript
  /* ---------------------------------------------------------
     Web Push подписка
  --------------------------------------------------------- */
  async function getPushSubscription() {
      if (!('serviceWorker' in navigator) || !('PushManager' in window)) return null;
      const reg = await navigator.serviceWorker.ready;
      return reg.pushManager.getSubscription();
  }

  async function subscribeToPush() {
      const reg = await navigator.serviceWorker.ready;
      const { publicKey } = await apiFetch('/push/vapid-key').then(r => r.json());
      const sub = await reg.pushManager.subscribe({
          userVisibleOnly: true,
          applicationServerKey: urlBase64ToUint8Array(publicKey),
      });
      await apiFetch('/push/subscribe', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ subscription: sub.toJSON() }),
      });
      return sub;
  }

  async function unsubscribeFromPush() {
      const sub = await getPushSubscription();
      if (sub) await sub.unsubscribe();
      await apiFetch('/push/subscribe', { method: 'DELETE' });
  }

  function urlBase64ToUint8Array(base64String) {
      const padding = '='.repeat((4 - base64String.length % 4) % 4);
      const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/');
      const raw = atob(base64);
      return Uint8Array.from([...raw].map(c => c.charCodeAt(0)));
  }
  ```

- [ ] **Step 3: Добавить UI в settings.html**

  Найди в `settings.html` раздел с уведомлениями (или добавь новый). Вставь:
  ```html
  <div class="settings-section">
    <h3>Push-уведомления</h3>
    <p style="color:var(--text-secondary);font-size:14px;margin-bottom:16px;">
      Получайте уведомления о новых КП, сообщениях и сделках прямо в браузере.
    </p>
    <button id="pushToggleBtn" class="btn btn-secondary" onclick="handlePushToggle()">
      Проверка...
    </button>
    <p id="pushStatusText" style="font-size:13px;color:var(--text-secondary);margin-top:8px;"></p>
  </div>
  ```

- [ ] **Step 4: Добавить скрипт в settings.html**

  В page-specific `<script>` в конце `settings.html` добавь:
  ```javascript
  async function initPushUI() {
      const btn = document.getElementById('pushToggleBtn');
      const status = document.getElementById('pushStatusText');
      if (!btn) return;
      if (!('serviceWorker' in navigator) || !('PushManager' in window)) {
          btn.textContent = 'Push не поддерживается браузером';
          btn.disabled = true;
          return;
      }
      const sub = await getPushSubscription();
      if (sub) {
          btn.textContent = 'Отключить Push-уведомления';
          status.textContent = 'Уведомления включены';
      } else {
          btn.textContent = 'Включить Push-уведомления';
          status.textContent = '';
      }
  }

  async function handlePushToggle() {
      const btn = document.getElementById('pushToggleBtn');
      const status = document.getElementById('pushStatusText');
      btn.disabled = true;
      try {
          const sub = await getPushSubscription();
          if (sub) {
              await unsubscribeFromPush();
              btn.textContent = 'Включить Push-уведомления';
              status.textContent = 'Уведомления отключены';
          } else {
              const permission = await Notification.requestPermission();
              if (permission !== 'granted') {
                  status.textContent = 'Разрешите уведомления в браузере';
                  btn.disabled = false;
                  return;
              }
              await subscribeToPush();
              btn.textContent = 'Отключить Push-уведомления';
              status.textContent = 'Уведомления включены';
          }
      } catch (e) {
          status.textContent = 'Ошибка: ' + e.message;
      }
      btn.disabled = false;
  }

  initPushUI();
  ```

- [ ] **Step 5: Проверить**

  ```bash
  npm test
  ```
  Ожидаемый результат: `Static checks passed: ...`

- [ ] **Step 6: Commit**

  ```bash
  git add assets/app.js settings.html
  git commit -m "feat: UI и логика подписки на Web Push уведомления в settings.html"
  ```

---

### Task 7: Push to main

- [ ] **Step 1: Финальная проверка**

  ```bash
  npm test
  ```

- [ ] **Step 2: Push**

  ```bash
  git push origin main
  ```

- [ ] **Step 3: На VPS — обновить .env и перезапустить**

  ```bash
  # На VPS:
  nano /var/www/neft/.env
  # Добавить VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT
  pm2 restart neft
  ```
